/** 
 * Ubuntu/Debian Essential Packages (libs)
 */

$ apt-get update
$ apt-get install build-essential libgtk2.0 libgtk2.0-dev libcurl-ocaml-dev libssl-dev



/**
 * Archlinux Essential Packages (libs)
 */

$ pacman -Syu
$ pacman -S base-devel liboauth curl openssl



/**
 * Download and install liboauth liboauth-0.9.4
 */
 
Download here ---> http://liboauth.sourceforge.net/
$ ./configure
$ make
$ make install (with super user)
 
 
 
/**
* Autoconfig & AutoMake - Install
*/

$ autoreconf --install
$ ./configure
$ make
$ make install (with super user)



/**
 * Autoconfig & AutoMake - Uninstalling
 */

$ make uninstall



/**
 * Compile (from Shell)
 */ 

$ gcc -Wall -DPACKAGE_PIXMAP_DIR=\""../img"\" -o twc twc.c oauth.c credits.c update_status.c other.c setting.c statusbar.c `pkg-config --cflags --libs gtk+-2.0 oauth`



/**
 * Compile (With Geany IDE)
 */

1) Choose more build actions
2) Set build menu commands
3) gcc -Wall -DPACKAGE_PIXMAP_DIR=\""../img"\" -o "%e" "%f" oauth.c credits.c update_status.c other.c setting.c statusbar.c `pkg-config --cflags --libs gtk+-2.0 oauth`
